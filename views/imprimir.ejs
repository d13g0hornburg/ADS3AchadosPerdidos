<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Imprimir Objeto</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="print-container">
        <div class="print-header">
            <h1>Protocolo do Objeto</h1>
        </div>
        <div class="print-item"><strong>Código:</strong> <%= objeto.id %></div>
        <div class="print-item"><strong>Descrição:</strong> <%= objeto.descricao %></div>
        <div class="print-item"><strong>Ambiente:</strong> <%= objeto.ambiente %></div>
        <div class="print-item"><strong>Professor:</strong> <%= objeto.professor %></div>
        <div class="print-item"><strong>Curso:</strong> <%= objeto.curso %></div>
        <div class="print-item"><strong>Data:</strong> <%= format(new Date(objeto.data), 'yyyy-MM-dd') %></div>
        <div class="print-item"><strong>Hora:</strong> <%= objeto.hora.slice(0, 5) %></div>
        <div class="print-item"><strong>Encontrado por:</strong> <%= objeto.encontrado %></div>
        <div class="print-item"><strong>Imagem:</strong><img src="/uploads/<%= objeto.imagem_objeto %>" alt="Imagem do Objeto" style="max-width: 50%; height: auto;"></div>
    </div>
    <div class="no-print" style="text-align: center; margin-top: 20px;">
        <button onclick="window.print()">Imprimir</button>
        <button onclick="window.close()">Fechar</button>
        <button onclick="window.location.href='/home'">Home</button>
    </div>
</body>
</html>
